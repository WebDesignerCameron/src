# Create
## Summary
Repository creation.
## Using
`create-repo.sh` creates a repository,
adds all files in the current
working directory, pushes the commit,
sets the GitHub Pages source as the
main branch and saves. Before doing
this, make sure to install GitHub
CLI and log in with the `auth login`
process. 
